# Database
# Required: PostgreSQL connection string
# Use Supabase, Vercel Postgres, or local PostgreSQL

# NextAuth Configuration
# Required: Application URL (use http://localhost:3000 for dev, https://yourdomain.com for prod)
NEXTAUTH_URL="http://localhost:3000"

# Required: Secret key for session encryption
# Generate with: openssl rand -base64 32
# Must be at least 32 characters long
NEXTAUTH_SECRET="Zq1Pu9351wLa19qG9Bjcf7qoYZ3o5WdtE5tyb+VRg48="

# Cron Security
# Required: Secret for protecting cron endpoints
CRON_SECRET="XiEB7fDDlwNTbFa1DR2vySgKGY9ycMQ7QKVbC5NT4CA="

# Email Server Configuration
# Required in PRODUCTION, Optional in DEVELOPMENT
# In development, magic links will fail but errors will be logged to console
# For production, configure SMTP settings (Gmail, SendGrid, etc.)

# SMTP Server Host (e.g., smtp.gmail.com, smtp.sendgrid.net)
EMAIL_SERVER_HOST="smtp.example.com"

# SMTP Port (usually 587 for TLS, 465 for SSL)
EMAIL_SERVER_PORT="587"

# SMTP Username (your email address or SMTP username)
EMAIL_SERVER_USER="your-email@example.com"

# SMTP Password (your email password or SMTP app password)
EMAIL_SERVER_PASSWORD="your-password"

# Email From Address (must match your SMTP account)
EMAIL_FROM="noreply@leagueintel.app"

# Optional: Sleeper API (currently uses public endpoints, no auth needed)
# SLEEPER_API_BASE_URL="https://api.sleeper.app"

# Local SQLite Database (no external services needed)
DATABASE_URL="file:./dev.db"
